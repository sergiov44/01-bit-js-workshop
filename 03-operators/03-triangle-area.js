// 04 Triangle Area

base = parseFloat(prompt("Type a number corresponding to the base of a triangle: "));
height = parseFloat(prompt("Type a number corresponding to the height of a triangle: "));
let result = (base * height) / 2;
console.log(`The area of the triangle is ${result}`);